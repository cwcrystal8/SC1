/*

strncpy(s, "hello", sizeOf(s));
   ensures that the array does not go beyond the memory allocated to it (n versions of all the string functions)

strncat(s1, s2, sizeOf(s1) - strlen(s2));

strcmp

*/
